{
  "name": "lyra",
  "private": true,
  "type": "module",
  "description": "Lyra AI — a Sui-native, policy-bound AI agent for autonomous DeFi.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "biome check .",
    "fix": "biome check --write .",
    "chat": "bun packages/cli/src/cli.ts",
    "agent": "bun packages/cli/src/cli.ts agent",
    "demo": "bun packages/cli/src/cli.ts demo",
    "gateway": "bun packages/gateway/src/server.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.3.14",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
