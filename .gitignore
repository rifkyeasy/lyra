# Dependencies
node_modules/

# Build artifacts
dist/
build/
*.tsbuildinfo

# Bun
# NOTE: commit bun.lock for reproducible CI installs (frozen-lockfile)

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/lib/
broadcast/
lcov.info

# Environment & secrets (NEVER commit keys)
.env
.env.local
.env*.local
*.key
*.pem
secrets/

# OS
.DS_Store
Thumbs.db

# Claude / agent context (local only)
.claude/

# Internal project knowledge (local-only research/strategy, not shipped)
knowledge/
knowledge-sui/

# Logs
*.log

# Local agent state (per-machine, never committed)
.lyra/
lyra.config.ts

# Test fixtures with private keys
test/fixtures/keys/

# Local integration tests (real API calls, may contain secrets/findings).
test/local/*
!test/local/tmux-*.ts
!test/local/_tmux.ts

# Editor
.vscode/
.idea/
*.swp

# Next.js build outputs (apps/web)
apps/web/.next/
apps/web/.vercel/
apps/web/out/
.vercel

# npm artifact from isolated apps/web installs (repo uses bun.lock)
apps/web/package-lock.json

# Hackathon submission prep (pitch deck, demo script, claims) — kept local only
submission/
.npmrc

# Local-only automation workspace (scripts / experiments) — not tracked
apps/automation/
apps/api/
apps/app/
apps/landing/
apps/waitlist-testnet/
apps/automate-article/

# The Move package lives in its own repo (lyraai-protocol/contracts). It is
# checked out here as a nested satellite repo, not tracked by the core repo.
move/

# Private testnet launch planning — internal, not committed
testnet-plan/
