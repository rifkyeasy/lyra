{
  "name": "lyra-plugin-onchain",
  "version": "0.5.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "The single on-chain plugin import for Lyra. Currently a v1 pass-through of lyra-plugin-onchain-one; the v2 (Wormhole bridge) half is deferred until it ships with npm-safe v1/v2 coexistence.",
  "exports": {
    ".": "./src/index.ts",
    "./protocol-ids": "./src/protocol-ids.ts"
  },
  "files": ["src", "!src/**/*.test.ts", "README.md"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "lyra-core": "workspace:*",
    "lyra-plugin-onchain-one": "workspace:*"
  }
}
